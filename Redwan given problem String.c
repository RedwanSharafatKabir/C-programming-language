#include<stdio.h>
int main()
{
    int Ara[100],i,n;
    char name[100];
    while(scanf("%d", &Ara)!=EOF)
      {
            while (scanf("%[^\n]",name)){
                      printf("%s\n",name);
                   }

      }

    return 0;
}
